add board(project)

-> = input
= = output/function
# = note

-> addboard <name> <deadline> # name of board and end date
= display default column config # should have 3 default column (Backlog, Active, Done)
= ask if user wants to keep default configs
-> Y/N
(if yes)
= continue
(if no)
= ask user to add columns
-> add column <name> # each columns needs an add function
= ask user to add tasks # default task limit = 5
-> addtask <name>



# each column has two column tracks for active and done